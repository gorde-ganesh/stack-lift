{
  "name": "@stack-lift/benchmarks",
  "version": "0.5.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "bench": "tsx src/index.ts"
  },
  "dependencies": {
    "@stack-lift/angular-provider": "workspace:*",
    "@stack-lift/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  }
}
